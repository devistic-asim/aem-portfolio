# Copy to .env and fill in (never commit .env)

# da.live upload — Bearer token from Adobe IMS
# How to get: sign in at https://da.live/ → DevTools → Network → any admin.da.live request → Authorization header
AEM_IMS_TOKEN=

# Helix preview/publish — API key from https://tools.aem.live/
HLX_AUTH_TOKEN=
